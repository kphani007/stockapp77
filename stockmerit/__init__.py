"""StockMerit 2.0 analysis package: signal features, scoring, and backtesting.

Kept framework-agnostic (plain pandas/numpy) so it can be called from the
current Streamlit app and from a future API service layer without change.
"""
